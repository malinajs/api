{
  "name": "malina-api",
  "version": "0.1.0",
  "description": "API for Malina.js resources",
  "main": "index.js",
  "scripts": {
    "build": "env-cmd --silent cfw build",
    "predeploy":"npm run build",
    "deploy": "env-cmd --silent cfw deploy"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "cfw": "^0.1.3",
    "dotenv": "^10.0.0",
    "env-cmd": "^10.1.0",
    "worktop": "^0.7.0"
  }
}
